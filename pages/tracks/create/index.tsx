import React from 'react';
import DefaultLayout from "../../layout/DefaultLayout";

const Create = () => {
    return (
        <DefaultLayout>
            Track List
        </DefaultLayout>
    );
};

export default Create;
