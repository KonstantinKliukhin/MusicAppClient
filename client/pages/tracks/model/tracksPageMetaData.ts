import { Metadata } from 'next';

export const tracksPageMetaData: Metadata = {
  title: 'Tracks list',
  description: 'Bsound tracks list',
  keywords: ['Bsound', 'music', 'tracks', 'sound', 'tracks list'],
};
