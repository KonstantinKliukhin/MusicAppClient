import CREATE_TRACK_PAGES_METADATA from './createTrackPagesMetaData';
import { ACCEPTED_AUDIO_TYPES, ACCEPTED_AUDIO_TYPES_STRING } from './acceptedAudioTypes';
import { ACCEPTED_IMAGE_TYPES, ACCEPTED_IMAGE_TYPES_STRING } from './acceptedImageTypes';

const constantsModule = {
  CREATE_TRACK_PAGES_METADATA,
  ACCEPTED_AUDIO_TYPES,
  ACCEPTED_AUDIO_TYPES_STRING,
  ACCEPTED_IMAGE_TYPES,
  ACCEPTED_IMAGE_TYPES_STRING,
};

export default constantsModule;
