import { Metadata } from 'next';

const CREATE_TRACK_PAGES_METADATA: Metadata = {
  title: 'Create track',
  description: 'Welcome to Bsound',
  keywords: ['Bsound', 'music', 'tracks', 'sound'],
};

export default CREATE_TRACK_PAGES_METADATA;
